# Optional if `modal` CLI is already authenticated via profile/config.
MODAL_TOKEN_ID=
MODAL_TOKEN_SECRET=
# Optional if the Modal workspace already has a secret named `huggingface-secret`.
HF_TOKEN=
HUGGINGFACE_TOKEN=
HUGGING_FACE_HUB_TOKEN=
TINYHARNESS_PROXY_TOKEN=
